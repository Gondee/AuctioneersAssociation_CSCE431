<p id="notice"><%= notice %></p>

<h1>Listing Pacs</h1>

<table>
  <thead>
    <tr>
      <th>ID</th>
      <th>Pac contribution</th>
      <th>member</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @pacs.each do |pac| %>
      <tr>
        <td><%= pac.id %></td>
        <td><%= pac.PAC_Contribution %></td>
        <td><%= pac.member_id %></td>
        <td><%= link_to 'Show', pac %></td>
        <td><%= link_to 'Edit', edit_pac_path(pac) %></td>
        <td><%= link_to 'Destroy', pac, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Pac', new_pac_path %>

<h2>Import Pacs From CSV</h2>
<p>The first line of the CSV must hold each columns' name, and should be consistent with the online table</p>
<%= form_tag import_pacs_path, multipart: true do %>
  <%= file_field_tag :file %>
  <%= submit_tag "Import" %>
<% end %>
